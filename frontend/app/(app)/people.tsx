import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const PeopleScreen = (props: Props) => {
  return (
    <View>
      <Text>PeopleScreen</Text>
    </View>
  );
};

export default PeopleScreen;

const styles = StyleSheet.create({});
